<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.eze.mapper.LikeMapper">

    <select id="checkLike" parameterType="com.spring.eze.board.dto.LikeDTO" resultType="int">
        SELECT COUNT(*) 
        FROM BOARD_LIKE_TBL 
        WHERE BNO = #{bno} AND USER_ID = #{userId}
    </select>

    <insert id="insertLike" parameterType="com.spring.eze.board.dto.LikeDTO">
        INSERT INTO BOARD_LIKE_TBL (
            LIKE_NO, 
            BNO, 
            USER_ID, 
            LIKE_DATE
        ) VALUES (
            SEQ_BOARD_LIKE.NEXTVAL, 
            #{bno}, 
            #{userId}, 
            SYSDATE
        )
    </insert>

    <delete id="deleteLike" parameterType="com.spring.eze.board.dto.LikeDTO">
        DELETE FROM BOARD_LIKE_TBL 
        WHERE BNO = #{bno} AND USER_ID = #{userId}
    </delete>

    <select id="getLikeCount" parameterType="int" resultType="int">
        SELECT COUNT(*) 
        FROM BOARD_LIKE_TBL 
        WHERE BNO = #{bno}
    </select>

</mapper>