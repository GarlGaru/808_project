<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.spring.eze.show.dao.Show.ShowDAO">  <!-- namespace="패키지명.인터페이스" -->

	<resultMap id="seatMap" type="com.spring.eze.show.dto.Show.ShowDTO">
		<id property="id" column="id"/>
		<result property="showMe" column="show_me"/>
	</resultMap>
	
	<!-- 메인 -->
	<select id="selectShowMainList" parameterType="map" resultType="com.spring.eze.show.dto.Show.ShowDTO">
    <choose>
        <when test="genre != null and genre != ''">
            SELECT * FROM (
                SELECT show_id as showId, 
                	   title, 
                	   venue_name as venueName, 
                	   poster_url as posterUrl, 
                	   start_date as startDate
                  FROM show_tbl
                 WHERE genre LIKE '%' || #{genre} || '%'
                 ORDER BY start_date DESC
            ) WHERE ROWNUM <![CDATA[ <= ]]> 20
        </when>
        
        <otherwise>
            SELECT * FROM (
                SELECT show_id as showId, 
                	   title, 
                	   venue_name as venueName, 
                	   poster_url as posterUrl, 
                	   start_date as startDate
                  FROM show_tbl 
                 WHERE genre LIKE '%대중음악%' 
                 ORDER BY start_date DESC
            ) WHERE ROWNUM <![CDATA[ <= ]]> 10
            UNION ALL
            SELECT * FROM (
                SELECT show_id as showId, 
                	   title, 
                	   venue_name as venueName, 
                	   poster_url as posterUrl, 
                	   start_date as startDate 
                  FROM show_tbl 
                 WHERE genre LIKE '%뮤지컬%' 
                 ORDER BY start_date DESC
            ) WHERE ROWNUM <![CDATA[ <= ]]> 8
            UNION ALL
            SELECT * FROM (
                SELECT show_id as showId, 
                	   title, 
                	   venue_name as venueName, 
                	   poster_url as posterUrl, 
                	   start_date as startDate 
                  FROM show_tbl 
                 WHERE genre LIKE '%연극%' 
                 ORDER BY start_date DESC
            ) WHERE ROWNUM <![CDATA[ <= ]]> 7
        </otherwise>
   	 	</choose>
	</select>
	
	<!-- 오픈 예정 공연 -->
	<select id="selectShowUpcoming" resultType="com.spring.eze.show.dto.Show.ShowDTO">
	SELECT * FROM (
        SELECT show_id AS showId,
               title,
               venue_name AS venueName,
               poster_url AS posterUrl,
               start_date AS startDate
          FROM show_tbl
         WHERE genre LIKE '%대중음악%'
           AND start_date > TO_DATE('2026-04-02', 'YYYY-MM-DD')
         ORDER BY start_date ASC
    ) WHERE ROWNUM <![CDATA[ <= ]]> 10

    UNION ALL

    SELECT * FROM (
        SELECT show_id AS showId,
               title,
               venue_name AS venueName,
               poster_url AS posterUrl,
               start_date AS startDate
          FROM show_tbl
         WHERE genre LIKE '%뮤지컬%'
           AND start_date > TO_DATE('2026-04-02', 'YYYY-MM-DD')
         ORDER BY start_date ASC
    ) WHERE ROWNUM <![CDATA[ <= ]]> 5

    UNION ALL

    SELECT * FROM (
        SELECT show_id AS showId,
               title,
               venue_name AS venueName,
               poster_url AS posterUrl,
               start_date AS startDate
          FROM show_tbl
         WHERE genre LIKE '%연극%'
           AND start_date > TO_DATE('2026-04-02', 'YYYY-MM-DD')
         ORDER BY start_date ASC
    ) WHERE ROWNUM <![CDATA[ <= ]]> 5
	</select>
	
	<!-- 장르별 상세페이지 -->
	<select id="getShowListByCategory" parameterType="java.util.Map" resultType="com.spring.eze.show.dto.Show.ShowDTO">
    SELECT * FROM (
        SELECT a.*, ROWNUM rnum
        FROM (
            SELECT s.show_id    AS showId,
                   s.title      AS title,
                   s.venue_name AS venueName,
                   s.start_date AS startDate,
                   s.genre      AS genre,
                   s.poster_url AS posterUrl
            FROM show_tbl s
            INNER JOIN showDetail_tbl d
                    ON s.show_id = d.show_id
            WHERE 1=1
              AND s.start_date > TO_DATE('2026-04-02', 'YYYY-MM-DD')
              AND s.show_id IN (
                    SELECT DISTINCT show_id
                    FROM schedule_tbl
              )
            <choose>
                <when test="category == 'concert'">
                    AND s.genre = '대중음악'
                </when>

                <when test="category == 'musical'">
                    AND s.genre = '뮤지컬'
                </when>

                <when test="category == 'play'">
                    AND s.genre = '연극'
                </when>

                <otherwise>
                    AND s.genre = '대중음악'
                </otherwise>
            </choose>

            ORDER BY s.start_date ASC
        ) a
        WHERE ROWNUM &lt;= 20
    )
    WHERE rnum &gt;= 1
	</select>
	
	<!-- 개별 상세페이지 -->
	<select id="getShowDetail" parameterType="String" resultType="com.spring.eze.show.dto.Show.ShowDTO">
	    SELECT 
	        s.show_id AS showId,
	        s.title,
	        s.venue_name AS venueName,
	        s.start_date AS startDate,
	        s.end_date AS endDate,
	        s.poster_url AS posterUrl,
	        s.genre,
	        d.age_limit AS ageLimit,       
	        d.ticket_price AS ticketPrice, 
	        d.cast_info AS castInfo,       
	        d.styurl,                      
	        d.runtime                      
	    FROM show_tbl s
	    LEFT OUTER JOIN showDetail_tbl d ON s.show_id = d.show_id
	    WHERE s.show_id = #{showId}
	</select>
	
	<!-- 개별 상세페이지 - 날짜, 회차 선택 -->
	<select id="getShowSchedule" parameterType="map" resultType="com.spring.eze.show.dto.Show.ShowDTO">
		SELECT 
	          schedule_id AS scheduleId,
	          start_time as startTime,
	          end_time as endTime,
	          show_id as showId,
	          play_date as playDate
	    FROM  schedule_tbl
<<<<<<< Updated upstream
	   
=======

>>>>>>> Stashed changes
	   WHERE  show_id = #{showId}
	    AND  TO_CHAR(play_date, 'YYYY-MM-DD') = #{playDate}
	   ORDER BY start_time 	
	</select>

	<!-- 랭킹페이지 -->
	<select id="getShowRanking" parameterType="String" resultType="com.spring.eze.show.dto.Show.ShowDTO">
		SELECT
			show_id AS showId,
			title,
			start_date AS startDate,
			end_date AS endDate,
			venue_name AS venueName,
			poster_url AS posterUrl,
			genre
	   FROM show_tbl
	  WHERE genre = #{genre}
	ORDER BY show_id DESC
	</select>

	<!--Seat 공연별 좌석도 때문에 추가합니다  -->
	<select id="selectVenueName" parameterType="String" resultType="String">
		SELECT venue_name
		FROM   show_tbl
		WHERE  show_id = #{showId}
	</select>

	<!-- 마이티켓 목록 -->
	<select id="getMyPageTicket" parameterType="long" resultType="com.spring.eze.show.dto.Myticket.MyticketDTO">
	SELECT
	        u.email AS email,
	
	        p.reservation_id AS reservationId,
	        p.user_id AS userId,
	        p.order_id AS orderId,
	        TO_CHAR(p.approved_at, 'YYYY.MM.DD') AS approvedAt,
	        p.quantity AS quantity,
	        p.status AS status,
	
	        r.total_price AS totalPrice,
	        r.payment_status AS paymentStatus,
	        r.payment_method AS paymentMethod,
	
	        s.title AS title,
	        s.poster_url AS posterUrl,
	        s.venue_name AS venueName,
	        s.start_date AS startDate,
	        s.end_date AS endDate,
	        s.start_date AS playDate,
	
	        CASE
	            WHEN s.start_date >= TRUNC(SYSDATE) THEN '가능'
	            ELSE '불가'
	        END AS cancelAvailable
	
	    FROM payment_order p
	    JOIN reservation_tbl r
	      ON p.reservation_id = r.reservation_id
	    JOIN user_tbl u
	      ON p.user_id = u.user_id
	    JOIN show_tbl s
	      ON r.show_id = s.show_id
	    WHERE p.user_id = #{userId}
	      AND p.payment_type = 'TICKET'
	      AND p.approved_at IS NOT NULL
	    ORDER BY p.approved_at DESC
	</select>
	
	<!-- 마이티켓 예매내역 카운팅 -->
	<select id="getMyTicketCount" parameterType="long" resultType="int">
		SELECT COUNT(*)
		  FROM payment_order p
		 WHERE p.user_id = #{userId}
		   AND p.payment_type = 'TICKET'
		   AND p.approved_at IS NOT NULL
	</select>
	<!-- seat회차 넘기는거때문에 추가합니다 -->
	<select id="selectScheduleInfo" parameterType="String" resultType="map">
		SELECT schedule_id,
			   show_id,
			   TO_CHAR(play_date, 'YYYY-MM-DD') AS play_date,
           	   TO_CHAR(play_date, 'DY', 'NLS_DATE_LANGUAGE=KOREAN') AS play_day,
			   start_time
		FROM   schedule_tbl
		WHERE  schedule_id = #{scheduleId}
	</select>
 </mapper>